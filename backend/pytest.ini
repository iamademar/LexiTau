[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration"
markers =
    integration: marks tests that require Postgres or dialect-specific behavior
pythonpath = .

[tool:pytest_asyncio]
asyncio_default_fixture_loop_scope = function